if(logging_DEFINE)
add_library(logging INTERFACE)
target_sources(logging INTERFACE logger.h)
target_link_libraries(logging INTERFACE spdlog)
target_include_directories(logging INTERFACE ./)
add_compile_definitions(logging_DEFINE)
endif()